Interface creation syntax

You use the `interface` keyword to define an interface.

You include an `implements` clause to declare a class that implements an interface. 

An interface definition typically contains constants and method signatures.

